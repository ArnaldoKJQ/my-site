{
  "name": "my-site",
  "version": "1.0.0",
  "type": "module",
  "description": "Portfolio + blog — Node.js static site generator",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve dist"
  },
  "dependencies": {
    "fs-extra": "^11.2.0",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  }
}
